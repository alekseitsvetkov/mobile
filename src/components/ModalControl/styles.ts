import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  modalControl: {
    backgroundColor: 'lightgrey',
    width: 50,
    height: 3,
    borderRadius: 100,
    position: 'absolute',
    top: 12,
  },
});
