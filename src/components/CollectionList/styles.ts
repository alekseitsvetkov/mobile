import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  collectionListTitle: {
    paddingHorizontal: 20,
    paddingVertical: 20,
    fontWeight: '600',
    fontSize: 16,
  },
});
