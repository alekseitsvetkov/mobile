import {StyleSheet} from 'react-native';

export const s = StyleSheet.create({
    imagePlaceholder: {
        borderRadius: 8,
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
});
